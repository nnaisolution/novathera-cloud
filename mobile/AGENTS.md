# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Navigation uses Expo Router (tabs + nested stacks). Session tokens must stay in expo-secure-store, never AsyncStorage.
